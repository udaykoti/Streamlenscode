# Optional. When unset, the frontend calls the same-origin path /api
# (Vite dev proxy -> localhost:8080). Set this to a deployed backend URL, e.g.:
# VITE_API_BASE_URL=https://streamlens-backend.example.com/api